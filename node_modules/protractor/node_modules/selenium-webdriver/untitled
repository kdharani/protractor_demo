
// iOS Mac Setup
	
	1. Install Brew (Package Manager for Mac OS)
		/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

	2. Install Carthage (Carthage is intended to be the simplest way to add frameworks to your Cocoa application.)
		brew install carthage

	3. Install Node & NPM (Node.js is an open source, cross-platform JavaScript runtime environment for developing a diverse variety of tools and applications. npm is the default package manager for the JavaScript runtime environment Node.js.)

		brew install node

	4. Install libimobiledevice (It allows other software to easily access the device's filesystem, retrieve information about the device and it's internals, backup/restore the device, manage SpringBoard® icons, manage installed applications, retrieve addressbook/calendars/notes and bookmarks)

		brew install libimobiledevice

	5. Install ios-deploy(Appium support for real devices running iOS 9.3 and above using Xcode 8+ also depends on ios-deploy)	
		npm install -g ios-deploy or brew install ios-deploy

	6. Install ideviceinstaller (Cross-platform library and tools for communicating with iOS devices. Only for iOS 9)
		brew install ideviceinstaller

	7. Install ios-webkit-debug-proxy (For Hybrid apps)
		brew install ios-webkit-debug-proxy

	8. Install Appium
		npm install -g appium

	9. Install Appium Doctor
		npm install appium-doctor

	10. Install xcpretty (Optional)   
		gem install xcpretty	


 	11. Install Command Line Tools
    		xcode-select —-install



// Install appium
	webdriver-manager update --android 
	webdriver-manager update --ios

//Build WebdriverAgentRunner

	xcodebuild -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner -destination 'id=bb5d42ba3257775cd5c32a6696338cf1443f78d3' test

// Enable mobile safari WebInspector
	Settings -> Safari -> Advanced 


// Real device capability

		platformName: 'ios',
		platformVersion:'10.3',
		deviceName:'iPhone 6s',
		automationName:'XCUITest',
		browserName: 'Safari',
		startIWDP:True,
		webkitResponseTimeout: 50000,
		safariInitialUrl:'https://www.accuweather.com',
		xcodeOrgId:'PTY3ERDG4R',
		xcodeSigningId:'iPhone Developer',
		udid: 'auto',
		autoWebView: true,
		newCommandTimeout: 3000,
		clearSystemFiles: true

// Simulator capability
        browserName: 'safari',
    	platformName: 'iOS',
    	platformVersion: '11.2',
    	deviceName: 'iPhone Simulator',
    	safariInitialUrl:'https://www.google.com/',
        autoWebView: true,
    	automationName:'XCUITest'


 // If webkit degbug proxy is not working

 		brew update
		brew reinstall --HEAD libimobiledevice
		brew reinstall -s ios-webkit-debug-proxy

